import { useRouteError } from "react-router-dom";

export default function Etc(){

    return (
        <div id="error-page">
            <h1>Etc</h1>
            <p>this is Etc😁😁</p>
        </div>
    );
}